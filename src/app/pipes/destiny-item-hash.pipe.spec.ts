import { DestinyItemHashPipe } from './destiny-item-hash.pipe';

describe('DestinyItemHashPipe', () => {
  it('create an instance', () => {
    const pipe = new DestinyItemHashPipe();
    expect(pipe).toBeTruthy();
  });
});
